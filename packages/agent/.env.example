# Environment configuration for the customer service agent

# Flask debug mode
FLASK_ENV=development

# Database configuration
# Options: firebase, postgresql
DATABASE_TYPE=firebase

# Choose 1 for Gemini Vertex AI or 0 for Gemini Developer API
GOOGLE_GENAI_USE_VERTEXAI=1

# Google Gemini Developer API Key (required if GOOGLE_GENAI_USE_VERTEXAI=0)
GOOGLE_API_KEY=your_google_api_key_here

# Vertex AI backend configuration (for AI processing)
GOOGLE_CLOUD_PROJECT=your_google_cloud_project
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=service-account.json

# Firebase/CRM configuration (for CRM database access)
FIREBASE_PROJECT=your_firebase_project
FIREBASE_CREDENTIALS=firebase-service-account.json

# PostgreSQL configuration (for future use)
# DATABASE_URL=postgresql://username:password@localhost:5432/asp_cranes_crm
# DATABASE_POOL_SIZE=10
# DATABASE_POOL_TIMEOUT=30
